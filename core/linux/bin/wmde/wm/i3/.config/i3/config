# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!


# Global-settings ==============================================================
# Mod-key-settings -------------------------------------------------------------
set $mod Mod4
# ------------------------------------------------------------------------------

# Global-font-settings -----------------------------------------------------
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 13
# font pango:SFNS Display 13
# ------------------------------------------------------------------------------
# ==============================================================================



# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet



# Colosr-Settings ==============================================================
# ------------------------------------------------------------------------------
set $bg-color 	         #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          #00ff00
client.unfocused        $inactive-bg-color  $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color  $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          #00ff00
# ------------------------------------------------------------------------------
# ==============================================================================



# Bar-Settings =================================================================
bar {
    # --------------------------------------------------------------------------
    # bar가 항상 보이게 / 숨김($mod 키를 누를때만 bar가 나타난다.)
    mode dock
    # mode hide
    # --------------------------------------------------------------------------

    # --------------------------------------------------------------------------
    # bar 위치 설정
    # position top
    position bottom
    # --------------------------------------------------------------------------

    # --------------------------------------------------------------------------
    # bar에 사용할 font를 설정

    # font pango:DejaVu Sans Mono 10

    # font pango:System San Francisco Display 13

    # 'FontAwesome'은 하위 호환용, '7 Free'와 '7 Brands'는 최신용입니다.
    # font pango:Noto Sans CJK KR 10, Font Awesome 7 Free 10, Font Awesome 7 Brands 10, FontAwesome 10
    # --------------------------------------------------------------------------

    # --------------------------------------------------------------------------
    # i3status / i3blocks를 사용한다.
    # status_command i3status
    # status_command i3blocks
    status_command i3blocks -c ~/.config/i3/i3blocks.conf
    # --------------------------------------------------------------------------

    # --------------------------------------------------------------------------
    colors {
        background $bg-color
        separator #757575
        #                  border             background         text
        focused_workspace  $bg-color          $bg-color          $text-color
        inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
        urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
    }
    # --------------------------------------------------------------------------
}
# ==============================================================================



# Gap-Settings =================================================================
# ------------------------------------------------------------------------------
# 창과 창 사이의 간격 (픽셀 단위) (효과가 크다)
gaps inner 8

# 창 묶음과 모니터 가장자리 사이의 간격 (효과가 크다)
# gaps outer 5
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# 창 타이틀바 표시
default_border normal 2

# floating 창 타이틀바 표시
default_floating_border normal 2

# 창 타이틀바 제거
# default_border pixel 2
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# 창이 하나만 있을 때는 간격을 자동으로 없애고 싶다면 (효과가 크다)
smart_gaps on

# 창이 하나만 있을 때는 테두리를 없애서 화면을 꽉 채운 느낌을 줌 (효과가 적다)
smart_borders on

# 좌우 테두리를 지워버려 화면이 훨씬 넓어 보이게 된다. (효과가 적다)
hide_edge_borders both
# hide_edge_borders smart
# ------------------------------------------------------------------------------
# ==============================================================================



# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar



# Hotkey =======================================================================
# 터미널 열기 --------------------------------------------------------------------
# ctrl + alt + t
# bindsym Control+Mod1+t exec i3-sensible-terminal
bindsym Control+Mod1+t exec xfce4-terminal
# ------------------------------------------------------------------------------

# 창닫기 -------------------------------------------------------------------------
# alt + F4
bindsym Mod1+F4 kill
# ------------------------------------------------------------------------------

# launcher 열기 -----------------------------------------------------------------
# start dmenu (a program launcher)
# bindsym $mod+r exec dmenu_run -nb black -sb tomato -l 10
# bindsym Mod1+F2 exec dmenu_run -nb black -sb tomato -l 10

# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# win + r
# alt + F2
bindsym $mod+r exec rofi -show drun -show-icons
bindsym Mod1+F2 exec rofi -show drun -show-icons
# bindsym Mod1+F2 exec rofi -show drun -lines 3 -eh 2 -width 100 -padding 800 -opacity "85" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$bg-color" -hlfg "#9575cd" -font "System SFNS Display 18"
# ------------------------------------------------------------------------------

# 포커스 설정 ---------------------------------------------------------------------
# win + ctrl + shift + 방향키
# bindsym $mod+Control+Shift+Left  focus left
# bindsym $mod+Control+Shift+Down  focus down
# bindsym $mod+Control+Shift+Up    focus up
# bindsym $mod+Control+Shift+Right focus right

# win + shift + hjkl
# bindsym $mod+Shift+h focus left
# bindsym $mod+Shift+j focus down
# bindsym $mod+Shift+k focus up
# bindsym $mod+Shift+l focus right

# alt + tab
bindsym Mod1+Tab focus next
# alt + shift + Tab
# bindsym Mod1+Shift+Tab focus prev
# ------------------------------------------------------------------------------

# 포커스 설정 (expose) -----------------------------------------------------------
# win + tab
# bindsym $mod+Tab exec "skippy-xd"
# bindsym $mod+Tab exec "rofi -show window -window-format '{w} {c} {t}' -theme-str 'window {width: 40%;}'"
# bindsym $mod+Tab exec "rofi -show window -show-icons"

bindsym $mod+Tab exec "/usr/bin/rofi -show window -theme '~/.config/rofi/themes/j_launcher.rasi'"

# rofi theme 설정
# rofi-theme-selector
# ------------------------------------------------------------------------------

# 타일링 (창이동) -----------------------------------------------------------------
# win + left
bindsym $mod+Left  move left
# win + shift + left
bindsym $mod+Shift+Down  move down
# win + shift + left
bindsym $mod+Shift+Up    move up
# win + right
bindsym $mod+Right move right

# alternatively, you can use the cursor keys:
# bindsym $mod+Alt_R focus left
# bindsym $mod+Super_R focus down
# bindsym $mod+Print focus up
# ------------------------------------------------------------------------------

# 전체화면 -----------------------------------------------------------------------
# F11
bindsym F11 fullscreen toggle
# ------------------------------------------------------------------------------

# 모드전환 -----------------------------------------------------------------------
# - stakcing
# win + s
# bindsym $mod+s layout stacking

# - tabbed
# win + up
bindsym $mod+Up layout tabbed

# - toogle split
# win + down
bindsym $mod+Down layout toggle split

# - 플로팅모드 toggle
# win + space
bindsym $mod+space floating toggle

# chage focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the parent container
# bindsym $mod+d focus child
# ------------------------------------------------------------------------------

# 창쪼개는 방향 -------------------------------------------------------------------
# - 수평 분할 (Horizontal):
# win + h
# bindsym $mod+Control+Shift+h split h

# - 수직 분할 (Vertical)
# win + v
# bindsym $mod+Control+Shift+v split v
# ------------------------------------------------------------------------------

# screenlock ------------------------------------------------------------------
# win + l
# bindsym $mod+l exec i3lock
bindsym $mod+l exec i3lock --color "$bg-color"
# ------------------------------------------------------------------------------

# printscreen ------------------------------------------------------------------
# print
# 방법1)
bindsym Print exec /usr/bin/gnome-screenshot --interactive

# 방법2)
# bindsym Print exec /usr/bin/xfce4-screenshooter
# ------------------------------------------------------------------------------

# 탐색기 -------------------------------------------------------------------------
# win + e
# bindsym $mod+e exec thunar
bindsym $mod+e exec pcmanfm
# bindsym $mod+e exec pcmanfm-qt
# ------------------------------------------------------------------------------

# taskmanager ------------------------------------------------------------------
# ctrl + shift + esc
bindsym Control+Shift+Escape exec xfce4-termianl -e htop
# ------------------------------------------------------------------------------

# 강제종료 -----------------------------------------------------------------------
# win + x
bindsym $mod+x exec /usr/bin/xkill
# ------------------------------------------------------------------------------

# 설정 재시작 --------------------------------------------------------------------
# i3 설정 파일 새로고침 (Restart i3 inplace)
# win + shift + c
# bindsym $mod+Shift+c reload

# win + shift + r
bindsym $mod+Shift+r restart
# ------------------------------------------------------------------------------

# logout -----------------------------------------------------------------------
# 작업 중인 모든 창이 닫히니 주의
# win + shift + e
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# control + alt + delete
bindsym Mod1+Control+Delete exec "i3-nagbar -t warning -m 'Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# ------------------------------------------------------------------------------

# workspace---------------------------------------------------------------------
# set workspace variables
set $ws1 "1: Web"
set $ws2 "2: Code"
set $ws3 "3: DCC"
set $ws4 "4: ETC"

# assign app to worspace
# xprop을 사용해서 class를 확인한다
assign [class="Chromium"] $ws1
assign [class="firefox"] $ws1
assign [class="firefox-esr"] $ws1
assign [class="Google-chrome"] $ws1
assign [class="Code"] $ws2
# assign [class="Mousepad"] $ws2

# switch to workspace
bindsym $mod+F1 workspace $ws1
bindsym $mod+F2 workspace $ws2
bindsym $mod+F3 workspace $ws3
bindsym $mod+F4 workspace $ws4
bindsym $mod+F5 workspace 5
bindsym $mod+F6 workspace 6
bindsym $mod+F7 workspace 7
bindsym $mod+F8 workspace 8
bindsym $mod+F9 workspace 9
bindsym $mod+F10 workspace 10

# move forcused container to workspace
bindsym $mod+Shift+F1 move container to workspace $ws1
bindsym $mod+Shift+F2 move container to workspace $ws2
bindsym $mod+Shift+F3 move container to workspace $ws3
bindsym $mod+Shift+F4 move container to workspace $ws4
bindsym $mod+Shift+F5 move container to workspace 5
bindsym $mod+Shift+F6 move container to workspace 6
bindsym $mod+Shift+F7 move container to workspace 7
bindsym $mod+Shift+F8 move container to workspace 8
bindsym $mod+Shift+F9 move container to workspace 9
bindsym $mod+Shift+F10 move container to workspace 10

# ctrl + wint + Left/Right로 이전/다음 워크스페이스 이동
bindsym Control+$mod+Left workspace prev
bindsym Control+$mod+Right workspace next

# (선택 사항) 창을 들고 다른 워크스페이스로 이동할 때: Ctrl + Alt + Shift + 방향키
# bindsym Control+Mod1+Shift+Left move container to workspace prev
# bindsym Control+Mod1+Shift+Right move container to workspace next
# ------------------------------------------------------------------------------
# ==============================================================================



# Autostart ====================================================================
# compositor -------------------------------------------------------------------
# exec --no-startup-id compton
# exec --no-startup-id compton -f

# exec --no-startup-id picom --backend xrender -b -f
# exec --no-startup-id picom --backend glx -b -f
# ------------------------------------------------------------------------------

# wallpapers -------------------------------------------------------------------
# set $wallpaper_path "/home/jungs/Pictures/wallpaper/aaronphs_indonesia.jpg"
set $wallpaper_path "/home/jungs/Pictures/wallpaper/dkidd_light.jpg"
# exec_always --no-startup-id feh --bg-scale $wallpaper_path
# ------------------------------------------------------------------------------

# resolution -------------------------------------------------------------------
# exec_always --no-startup-id xrandr --output eDP-1 --primary --mode 1368x768 --pos 0x0 --rotate normal --output VGA-1 --off --output HDMI-1 --off --output DP-1 --off
# ------------------------------------------------------------------------------

# 부팅시 workspace에 app 자동 실행 -------------------------------------------------
# exec i3-msg 'workspace $ws1; exec /usr/bin/google-chrome'
# exec i3-msg 'workspace $ws1; exec /usr/bin/chromium'
# exec i3-msg 'workspace $ws2; exec /usr/bin/mousepad'
# ------------------------------------------------------------------------------

# IME --------------------------------------------------------------------------
exec --no-startup-id /usr/bin/fcitx5
# ------------------------------------------------------------------------------

# clipboard --------------------------------------------------------------------
# exec --no-startup-id xfce4-clipman &
# ------------------------------------------------------------------------------
# ==============================================================================